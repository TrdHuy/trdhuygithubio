# trdhuygithubio

### how to build
- install needed packages 
     ```cmd
     npm install 
     ```
- run clean command:
     ```cmd
     npm run clean
     ```
- run build debug version:
     ```cmd
     npm run buildDebug
     ```
- run build release version:
     ```cmd
     npm run buildRelease
     ```
